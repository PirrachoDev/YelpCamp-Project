<% layout('/layouts/boilerplate') %>
<div id="cluster-map"></div>
    <h1>All Campgrounds</h1>
    <% for (let campground of campgrounds) { %>
        <div class="card mb-3">
            <div class="row">
                <div class="col-md-4">
                    <a href="/campgrounds/<%=campground.id%>">
                    <% if (campground.images.length) {%> 
                        <img class="img-fluid" src="<%= campground.images[0].url %>" alt="">
                    <% } else { %> 
                        <img class="img-fluid" 
                        src="https://res.cloudinary.com/pirrachodev/image/upload/v1662004504/YelpCamp/wsjczux4lplrd59npshr.png" 
                        alt="default image">
                    <% } %> 
                    </a>
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title"><a class="text-decoration-none link-dark" href="/campgrounds/<%=campground.id%>"><%= campground.title %></a></h5>
                        <p class="card-text"><%= campground.description %></p>
                        <p class="card-text">
                            <small class="text-muted"><%= campground.location %></small>
                        </p>
                        <a class="btn btn-primary" href="/campgrounds/<%=campground.id%>">View <%= campground.title %></a>
                    </div>
                </div>
            </div>
        </div>
    <% } %>
    <div class="pagination2022">
      <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
          <li class="page-item"><a class="page-link" href="/campgrounds?page=<%=parseInt(currentPage) - 1%>">Previous</a></li>
          <% for (let i = 0; i < 5; i++) { %>
            <% let count = parseInt(currentPage) + i; %>
            <li class="page-item"><a class="page-link" href="/campgrounds?page=<%=count%>"><%=count%></a></li>
          <% } %>
          <li class="page-item"><a class="page-link" href="/campgrounds?page=<%=parseInt(currentPage) + 5%>">Next</a></li>
        </ul>
      </nav>
    </div>
<script>
    const mapToken = "<%- process.env['MAPBOX_TOKEN'] %>";
    const campgrounds = {features: <%- JSON.stringify(campgrounds) %>};
</script>
<script src="JS/clusterMap.js"></script>